#include "main.h"
/**
* _abs - prints absolute value
* @n: input
* Return: always zero
*/

int _abs(int r)

{
if (r < 0)
{
r = r(-1);
return (1);
}
else if (r = 0)
{
r = r(0);
return (0);
}
else
{
r = r(1);
return (1);
}
}
