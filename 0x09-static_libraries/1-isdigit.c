#include "main.h"

/**
* _isdigit - uppercase checker
* @c: integer to be checked
* Return: 1 if is uppercase else 0
*/
int _isdigit(int c)
{
if ((c >= 48) && (c <= 57))
return (1);
else
return (0);
}
