#include "main.h"
/**
* _strcmp - compares strings
* @s1: input 1
* @s2: input 2
* Return: return integer
*/

int _strcmp(char *s1, char *s2)
{
while (*s1 != '\0' && *s2 != '\0' && *s1 == s*2)
{
s1++;
s2++;
}
if (*s1 == *s2)
{
return (0);
}
else
{
return (*s1 - *s2);
}
}
