#include "main.h"
/**
* _puts_recursion - prints string
* @s: pointer to string
* Return: nothing
*/

void _puts_recursion(char *s)
{
  int i;
  for (i = 0; s[i] != '\0'; i++)
    {
      _putchar(*s);
    }
  _putchar('\n');
}
